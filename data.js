names = ['yogii','virat','suriya','undefined'];

// console.log(names); // while prointing

// console.log('hi this is yogesh')

ages = [27,22,21];

module.exports = {names,ages};  //we can export string or ing directly -  we using exports in plural to export multiple data or lists  

//by default if nothing is exported it returns an empty object so if multiple exports are there we use to send them as a single object
//in case any key is not set in tha object it returns the current key with value  if key is defined in exports then that will display